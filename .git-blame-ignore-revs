# Commits that only reformat code. git blame skips them when enabled with
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
2405974fa3dc067b176f043dcdf09ab243eda8e5  # format: apply clang-format to src/ and tests/
